{
  "name": "tty2oled-gc9a01-web",
  "description": "WebSerial marquee editor/gallery for the tty2oled-gc9a01 firmware - crop, preview, and send per-core marquee art to an ESP32-C3 + GC9A01 round-display tty2oled over WebSerial.",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/doccaz/mister-tty2oled-gc9a01.git",
    "directory": "web"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
